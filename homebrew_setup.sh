# homebrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
echo "[homebrew setup] brew installed"

brew tap homebrew/bundle
brew bundle install

echo "[homebrew setup] brew bundle installed"

# brew bundle
brew bundle
echo "[homebrew setup] bundle installed"
